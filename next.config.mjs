/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "jp-nusantara.com", "plus.unsplash.com", "i.pinimg.com"],
  },
};

export default nextConfig;
